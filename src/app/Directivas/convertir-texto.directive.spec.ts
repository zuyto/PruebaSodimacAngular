import { ConvertirTextoDirective } from './convertir-texto.directive';

describe('ConvertirTextoDirective', () => {
  it('should create an instance', () => {
    const directive = new ConvertirTextoDirective();
    expect(directive).toBeTruthy();
  });
});
