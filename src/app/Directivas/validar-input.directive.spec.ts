import { ValidarInputDirective } from './validar-input.directive';

describe('ValidarInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidarInputDirective();
    expect(directive).toBeTruthy();
  });
});
