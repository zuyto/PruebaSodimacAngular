import { LongitudInputDirective } from './longitud-input.directive';

describe('LongitudInputDirective', () => {
  it('should create an instance', () => {
    const directive = new LongitudInputDirective();
    expect(directive).toBeTruthy();
  });
});
